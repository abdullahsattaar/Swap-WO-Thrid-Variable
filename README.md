# Swap-WO-Thrid-Variable
A C++ function that receives two integer pointers and swap their values without using third variable.  Call the function passing the addresses of two integer variables in the main.
